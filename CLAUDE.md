# CLAUDE.md

이 저장소에서 작업하는 AI 에이전트를 위한 작업 지시서. 황재진(이하 "작성자")과
합의한 규칙을 담는다. 작업 전에 반드시 읽고, 규칙과 충돌하는 요청을 받으면
임의로 판단하지 말고 작성자에게 확인한다.

**이 문서는 살아 있는 문서다.** 규칙이 바뀌거나 새 결정이 생기면 해당 절을
그 자리에서 갱신한다. 자세한 방법은 8절.

---

## 0. 저장소 개요

- 공부한 내용을 정리해 기록하는 개인 기술 블로그. 주제는 **Physical AI**.
- **Astro 7** + **Tailwind v4**. 정적 사이트, GitHub Actions로 GitHub Pages 배포.
- 글은 `_posts/`에 마크다운으로 있다. Jekyll 시절의 위치를 그대로 쓴다.
- 한국어로 쓴다. 사이트 언어는 `ko-KR`.
- `https://jayrobotics.github.io`

---

## 1. 원칙

### 1.1 승인 권한은 작성자에게 있다

- **요청받은 범위만 작업한다.** 범위 밖의 개선점은 **제안만** 하고 승인 없이
  실행하지 않는다.
- 되돌리기 어려운 것 (URL, 파일명, 카테고리 이름, 글 내용, `main` 커밋, push)은
  예외 없이 승인을 받는다.
- 제안은 근거와 비용을 같이 제시한다. "더 좋아 보인다"는 근거가 아니다.
- 승인 없이 한 작업을 발견하면 되돌리고 보고한다.

### 1.2 테마는 Physical AI다

독자는 로보틱스 연구자와 채용담당이다. 이들을 설득하는 것은 마케팅 광택이 아니라
**기술적 신뢰도**다. 절제된 계측기/논문 계열의 언어를 지향한다.

그리고 이 독자가 평가하는 곳은 랜딩 페이지가 아니라 **글 페이지**다. 수식 조판,
코드 가독성, 그림 설명, 구조. 디자인 작업의 비중을 거기에 싣는다.

### 1.3 기존 구현을 먼저 파악한다. 중복 금지

**새 기능을 만들기 전에 이미 있는지 확인한다.** 2절을 읽고 필요하면 파일을 연다.

- 같은 문제를 두 곳에서 풀지 않는다.
- 기능을 없애거나 대체할 때는 남는 죽은 코드(CSS, 클래스, 데이터)도 같이 지운다.

> 실패 사례: 홈에 카테고리 필터를 만들려 한 적이 있다. 카테고리 탭이 이미 같은
> 일을 하고 있었고, 그 대가로 페이지네이션을 버릴 뻔했다. 되돌렸다.

### 1.4 독자의 가독성과 가시성을 우선한다

구체적인 규약은 5절.

### 1.5 영어 용어는 원문을 쓴다

한글로 옮기기 까다롭거나, 옮기면 뜻이 모호해지는 용어는 **영어 원문 그대로**.

- 그대로: `reward`, `policy`, `Markov Decision Process`, `bucket`,
  `hash collision`, `overflow`, `chaining`, `heuristic`
- 한글로: 정렬, 기수, 자릿수, 가상환경, 커널
- 한 글 안에서 같은 개념을 번갈아 쓰지 않는다. 처음에 `보상(reward)`으로 병기하고
  이후 한쪽으로 통일한다.

**UI 문구도 같은 기준이다.** 한국어 독자가 읽는 섹션 제목을 영어 대문자 라벨로
바꾸지 않는다 (`최근 글`을 `RECENT`로 바꾸지 않는다). 장식을 위해 읽기를 버리는
교환이다. 짧은 영문 약어 배지(`RL`, `Algo`)는 기능적이므로 예외.

---

## 2. 구조

| 경로 | 역할 |
|---|---|
| `_posts/**/*.md` | 글. Astro의 glob 로더가 여기서 직접 읽는다 |
| `_tabs/about.md` | 정보 페이지 본문 |
| `src/content.config.ts` | 컬렉션 스키마와 `generateId` |
| `src/lib/posts.ts` | 정렬, 그룹화, `slugify`, 날짜 포맷 |
| `src/lib/headings.ts` | 목차에 넣을 제목 선별과 텍스트 복원 |
| `src/lib/rehype-post.mjs` | 그림 캡션, 코드 언어 라벨 |
| `src/styles/global.css` | 토큰, 본문 조판 |
| `src/styles/toc.css` | 목차 |
| `src/styles/shelf.css` | 홈 연재 선반 |
| `src/data/categoryLabels.ts` | 목록의 카테고리 배지 라벨 |
| `scripts/*.mjs` | 검증기 (7절) |
| `public/assets/images/` | 이미지. `/assets/images/...`로 서빙된다 |

### 이미 있으니 다시 만들지 말 것

카테고리/태그 탐색(`/categories/`, `/tags/`), 아카이브, 목차, 검색(예정),
다크모드 토글, 그림 캡션, 코드 라벨.

홈의 연재 선반은 **쇼케이스이지 네비게이션이 아니다.** 탐색 기능을 홈에
복제하지 않는다.

---

## 3. URL은 불가침이다

**이것이 이 저장소에서 가장 강한 제약이다.**

`_config.yml`의 utterances 설정이 `issue_term: "pathname"`이다. 댓글이 **경로로**
GitHub 이슈에 묶여 있다. 경로가 바뀌면 그 글의 댓글이 사라진 것처럼 보이고,
**로컬 빌드로는 절대 안 잡힌다.**

- **글 URL은 파일명에서 나온다.** front matter의 `title`이 아니다.
  `2026-06-14-OSSRL-01.md` → `/posts/OSSRL-01/`.
  **제목 수정은 URL에 영향이 없고, 파일명 변경은 URL을 바꾼다.**
- `src/content.config.ts`의 `generateId`가 날짜 접두어만 떼고 **대소문자를
  보존한다.** glob 로더 기본값은 소문자로 바꾸므로 글 16편이 전부 이동한다.
- 카테고리·태그 URL은 `slugify()`가 만든다. Jekyll이 쓰던 규칙을 그대로
  재현한 것이고, `scripts/check-slugs.mjs`가 이를 검증한다. **함부로 고치지
  않는다.**
- 페이지네이션은 `/page2/`이지 `/page/2/`가 아니다. **다만 이전/다음은 이동이
  아니라 제자리 전환이다.** 목록 다섯 줄을 넘기려고 페이지를 다시 불러오면
  읽던 자리를 버리게 된다. `/pageN/` 라우트는 매니페스트에 있으므로 남겨두고,
  그 페이지에서 시작할 위치만 정한다.
- `url-manifest.txt`는 Jekyll이 서빙하던 URL 80개다. **이주의 합격 기준**이며
  `scripts/check-urls.mjs`가 대조한다.

---

## 4. 디자인 규약

### 색

- **액센트는 하나다.** electric blue: 라이트 `#0b5ed7`, 다크 `#6ea8fe`.
  섹션마다 다른 색을 쓰지 않는다.
- **순수 `#ffffff` / `#000000`을 쓰지 않는다.** 긴 기술 문서에서 순백은 눈부시고
  순흑은 깊이를 죽인다.
- **중성색은 차갑게.** 액센트가 파랑이므로 따뜻한 회색(크림·베이지)과 섞으면
  한 페이지 안에서 색온도가 싸운다.
- **표면 단계는 둘.** `--surface`, `--surface-2`. 코드·표·목차는 페이지 위로
  뜨는 게 아니라 **안으로 들어가야** 한다.
- **다크모드는 1급이다.** 반전이 아니라 별도 설계. 연구자는 밤에 읽는다.
- 대비는 WCAG AA 이상. `scripts/check-contrast.mjs`가 검사한다.

### 타이포그래피

- **본문은 Pretendard.** 시스템 기본 스택에는 한글 폰트가 없어 중국어 폰트로
  폴백된다.
- **한글 줄바꿈은 `word-break: keep-all`.** 없으면 단어 중간에서 잘린다.
- **모든 숫자는 JetBrains Mono, tabular.** 날짜, 개수, 표, 본문 속 수치.
  `.num` 유틸리티를 쓴다. 문장 속 숫자와 표 속 숫자의 자릿수가 맞아야 한다.
  네이티브 모노 스택은 OS마다 달라서 그 정밀함이 깨진다.
- 섹션 구분은 **전체 폭 헤어라인**이다. 제목 옆 색 탭이 아니다.

### 구조

- **상자보다 헤어라인.** 표는 셀 격자 대신 행 아래 선만. 카드는 위계가 실제로
  있을 때만.
- 모서리 반경은 한 체계로 (0.4~0.5rem 계열).
- 본문 폭은 **42rem**. 넘어가면 한 줄이 너무 길어 읽기가 무너진다.
- 목차 레일은 **1200px 이상**에서만. 그 아래는 가장자리 목차.

### 모션

- **`transform`과 `opacity`만 애니메이션한다.** 높이·폭·위치를 애니메이션하면
  리플로우가 생기고 그게 "산만함"의 정체다.
- `display`는 전환되지 않는다. 토글하면 한 프레임에 튄다.
- 모든 전환은 `prefers-reduced-motion: reduce`에서 꺼져야 한다.
- 스크롤 리스너 대신 `IntersectionObserver`.
- 목차 이동은 `scroll-behavior: smooth` + 제목에 `scroll-margin-top: 6rem`.

---

## 5. 글 작성 규약

### Front matter

```yaml
---
title: "[시리즈명] NN. 제목"
date: YYYY-MM-DD HH:MM:SS +0900
categories: [카테고리]
tags: [태그, 태그]
comments: true
math: true
toc: true
image:
  path: /assets/images/<시리즈>/<챕터>/<썸네일>.png
---
```

- 제목의 `[시리즈명]` 접두어는 목록에서 자동으로 떨어진다.
- 이미지 경로는 `public/assets/images/<시리즈>/<챕터>/`.
- `pin: true`는 한 번에 한 글만.
- 날짜는 문자열로 파싱되어 `z.coerce.date()`로 변환된다. `+0900` 오프셋은
  정확히 반영되지만, **표시는 반드시 `Asia/Seoul`로 포맷**해야 자정 근처 글의
  날짜가 안 틀어진다.

### 본문

- **여백용 `<br>`을 쓰지 않는다.** 간격은 CSS가 만든다. 문장 중간의 진짜
  줄바꿈에만 쓴다.
- `##`(h2)로 섹션을 나눈다. **제목에는 번호를 붙인다** (`1.`, `1-1.`, `1-1-1.`).
  목차가 번호 있는 제목만 담기 때문이다 (6절).
- 색을 직접 지정한 `<span style="color:...">`는 다크모드에서 깨지므로 새로
  쓰지 않는다. 강조는 `<mark>` 또는 굵게.
- 수식은 `math: true` + `$...$` / `$$...$$`. KaTeX가 렌더한다.
- 출처는 `## References`로 글 하단에 둔다.

### 그림과 표 캡션

- **이미지의 `alt`가 곧 캡션이다.** `src/lib/rehype-post.mjs`가 `<figcaption>`
  으로 꺼내 보여준다. `image`, `screenshot` 같은 일반적인 값은 제외된다.
- **번호는 손으로 쓰고, 글 바깥의 무언가를 가리킬 때만 붙인다.**
  알고리즘 글의 `그림 6.3`은 교재(최영규, 『파이썬 알고리즘』)의 그림 번호라서
  의미가 있다. RL 글의 강의 캡처는 가리키는 대상이 없으므로 번호를 붙이지
  않는다. 격식을 위한 번호는 관리 비용만 남는다.
- 표기는 `그림 N.` / `표 N.` (한글). 본문이 한국어라 "그림 6.3에서 보듯"으로
  자연스럽게 참조된다.
- **표에는 캡션을 달지 않는다.** 바로 위 문장이 이미 설명하고 있다.

---

## 6. 목차 규약

- **번호로 시작하는 제목만 담는다** (`1.`, `1-1.`, `1-1-1.`). 마침표는 선택이고,
  숫자 뒤 공백은 필수다. `3가지 reward type`은 제외되고 `3-2 공간 복잡도`는
  포함된다.
- **`References`는 번호가 없어도 포함한다.** 참고문헌은 문서 구조의 일부다.
- **들여쓰기는 절대 깊이가 아니라 "그 글이 실제로 쓴 깊이의 순번"이다.**
  RL 글은 `h2 → h4`, 알고리즘 글은 `h2 → h3`로 같은 수준을 다르게 쓴다.
  깊이 그대로 들여쓰면 한쪽만 두 배로 밀린다.
- **제목 텍스트는 원본 마크다운에서 읽는다.** KaTeX가 수식을 3중(MathML, HTML,
  주석)으로 렌더해서 `$P$`가 `PPP`로 나온다.
- 가장자리 목차는 **대시 레이어와 패널 레이어가 분리되어 있다.** 하나로 합치면
  "접혔을 때 촘촘함"과 "펼칠 때 안 움직임"을 동시에 만족할 수 없다.

---

## 7. 작업 절차

```bash
npm run dev      # 개발 서버 (astro dev stop / status / logs 로 관리)
npm run build    # 빌드만
npm run verify   # 빌드 + 검증기 3개. 보고 전에 이걸 돌린다
```

`verify`가 돌리는 것:

| 검증기 | 무엇을 막는가 |
|---|---|
| `check-urls.mjs` | URL 80개 중 하나라도 사라지는 것. **가장 중요하다** |
| `check-slugs.mjs` | `slugify`가 기존 카테고리·태그 URL을 못 만드는 것 |
| `check-contrast.mjs` | 색을 눈으로 골라 대비가 무너지는 것 |

> **현재 `verify`는 `check-urls`에서 실패한다.** `/feed.xml`, `/sitemap.xml`,
> `/robots.txt` 셋이 아직 없기 때문이고, 이주 5단계에서 만들면 통과한다.
> 그때까지는 **missing이 정확히 그 셋인지**를 확인하면 된다. 다른 URL이 섞여
> 나오면 실제 문제다.

작업을 끝냈다고 보고하기 전에:

1. **빌드가 경고 없이 끝나는가.**
2. **`check-urls.mjs`가 통과하는가.** 이게 가장 중요하다.
3. 색을 건드렸으면 **`check-contrast.mjs`**.
4. slug·카테고리·태그를 건드렸으면 **`check-slugs.mjs`**.
5. **라이트/다크 양쪽에서 확인했는가.**
6. **세로 화면(1200px 미만)에서 깨지지 않는가.**
7. **죽은 코드를 남기지 않았는가.**

검증하지 않은 것을 "됐다"고 보고하지 않는다. 못 한 검증은 못 했다고 말한다.

### 이 환경의 함정

- **`node_modules/.astro` 캐시.** rehype 플러그인을 고쳐도 반영이 안 되면
  여기를 지운다. `.astro`와 `dist`만으로는 부족하다.
- **heredoc이 백슬래시를 먹는다.** 따옴표로 감싸도 `\\s`가 `\s`로 붕괴한다.
  정규식이 들어가는 파일은 Write 도구로 쓴다.
- **Astro 7 기본 마크다운 처리기는 Sätteri다.** 수식을 파싱만 하고 KaTeX
  렌더러가 없다. `unified()` 처리기를 써야 remark/rehype가 붙는다.
- **`rehypeRaw`가 사용자 플러그인보다 뒤에 있다.** 마크다운 속 raw HTML은 그
  시점에 문자열이다. `astro.config.mjs`에서 앞에 한 번 더 돌린다.

---

## 8. 변경 기록 규칙

**결정이 바뀌면 해당 절을 그 자리에서 고친다.** 문서 끝에 변경 이력을 쌓지
않는다. 이력은 git이 이미 갖고 있고, 두 곳에 두면 갈라진다.

기록할 것과 안 할 것:

| 기록한다 | 기록하지 않는다 |
|---|---|
| 앞으로도 지켜야 할 규칙 | 한 번의 버그 수정 |
| 그렇게 정한 **이유** | 코드를 보면 아는 사실 |
| 시도했다가 틀린 것으로 밝혀진 접근 | 작업 진행 상황 |
| 환경의 함정 | git log로 알 수 있는 것 |

**이유를 같이 적는다.** "순백을 쓰지 않는다"만 적으면 다음 사람이 되돌린다.
"긴 기술 문서에서 눈부시다"까지 적어야 규칙이 산다.

규칙을 바꾸거나 추가했으면 **작업 보고에 무엇을 어느 절에 적었는지 한 줄로
말한다.** 조용히 고치지 않는다.

---

## 9. 하지 말 것

- 승인 없는 `main` 커밋과 `git push`.
  (리빌딩 기간 동안 `astro-migration` 브랜치 커밋은 임시 허용됨)
- URL, 글 파일명, 카테고리 이름을 조용히 바꾸기.
- 작성자가 쓴 글의 내용을 임의로 수정. 오탈자도 보고 후 승인받는다.
- 확인 안 된 정보를 글이나 About 페이지에 쓰기. 모르면 빈칸으로 두고 묻는다.
- 새 의존성 추가. 몇 줄로 되는 일에는 더더욱.
- 높이·폭·위치를 애니메이션하기.
- 한국어 UI 문구를 영어 장식 라벨로 바꾸기.
