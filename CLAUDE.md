# CLAUDE.md

이 저장소에서 작업하는 AI 에이전트를 위한 작업 지시서. 황재진(이하 "작성자")과
합의한 규칙을 담는다. 작업 전에 반드시 읽고, 규칙과 충돌하는 요청을 받으면
임의로 판단하지 말고 작성자에게 확인한다.

이 문서는 계속 추가된다. 새 규칙이 생기면 해당 절에 덧붙인다.

---

## 0. 저장소 개요

- 공부한 내용을 정리해 기록하는 개인 기술 블로그. 주제는 **Physical AI**.
- Jekyll + `jekyll-theme-chirpy` **7.4.1** (gem 설치). 테마 파일 일부를
  `_layouts/`, `_includes/`에 복사해 수정한 상태다.
- 사이트 언어는 `ko-KR`. 글은 한국어로 쓴다.
- 배포는 GitHub Pages (`https://jayrobotics.github.io`).

---

## 1. 원칙

### 1.1 승인 권한은 작성자에게 있다

이 블로그의 모든 결정권은 작성자에게 있다.

- **요청받은 범위만 작업한다.** 범위 밖의 개선점을 발견하면 **제안만** 하고,
  승인 없이 실행하지 않는다.
- 작업 도중 "이것도 고치는 게 낫겠다"는 판단이 들어도 멈추고 묻는다.
  특히 되돌리기 어려운 것 (URL 구조, 카테고리 이름, 글 내용, git push)은 예외 없다.
- 제안은 근거와 비용을 같이 제시한다. "더 좋아 보인다"는 근거가 아니다.
- 승인 없이 한 작업을 발견하면 되돌리고 보고한다.

### 1.2 테마는 Physical AI다

디자인은 이 정체성에 맞춰야 한다. **필요하면 바꾸려고 시도해야 한다.**
"테마 기본값이라서" 또는 "건드리면 복잡해서"는 현상 유지의 이유가 되지 않는다.
디자인 변경도 1.1의 승인 규칙을 따른다. 구체적인 규약은 3절.

### 1.3 기존 구현을 먼저 파악한다. 중복 금지

**새 기능을 만들기 전에 이미 있는지 확인한다.** 2절의 인벤토리를 읽고,
필요하면 실제 파일을 열어본다.

- 같은 문제를 두 곳에서 풀지 않는다.
- 테마가 이미 제공하는 기능을 다시 구현하지 않는다.
- CSS 클래스, Liquid 로직, 데이터 파일 모두 해당된다.
- 기능을 없애거나 대체할 때는 남는 죽은 코드(CSS, 클래스, 데이터)도 같이 지운다.

> 실패 사례: 홈에 카테고리 필터를 만들려 한 적이 있다. 카테고리 탭이 이미
> 같은 일을 하고 있었고, 그 대가로 페이지네이션을 버릴 뻔했다. 되돌렸다.

### 1.4 독자의 가독성과 가시성을 우선한다

글은 읽히기 위해 쓴다. 구체적인 규약은 4절.

### 1.5 영어 용어는 원문을 쓴다

한글로 옮기기 까다롭거나, 옮기면 뜻이 모호해지는 용어는 **영어 원문 그대로** 쓴다.

- 그대로: `reward`, `policy`, `Markov Decision Process`, `bucket`,
  `hash collision`, `overflow`, `chaining`, `heuristic`
- 한글로: 정렬, 기수, 자릿수, 가상환경, 커널처럼 한국어가 이미 정착한 용어
- 한 글 안에서 같은 개념을 한글과 영어로 번갈아 쓰지 않는다. 처음 나올 때
  `보상(reward)`처럼 병기하고, 이후에는 한쪽으로 통일한다.

---

## 2. 기존 구현 인벤토리

테마 원본과 **다른** 파일만 적는다. 나머지는 Chirpy 7.4.1 기본 동작이다.
이 목록은 기능을 추가/삭제할 때마다 갱신한다.

| 파일 | 내용 |
|---|---|
| `_layouts/home.html` | 홈 렌더링 교체. 인트로 + "최근 연재" 선반 + 압축 목록. Chirpy의 pin/페이지네이션 조립 로직(상단 49줄)은 그대로 둔다 |
| `_layouts/post.html` | 기본 `post-nav`(이전/다음 글) 비활성화. 대신 `related-posts` 사용 |
| `_includes/related-posts.html` | 글 하단의 같은 카테고리 목록. 5개씩 클라이언트 페이징, 현재 글 표시 |
| `_includes/sidebar.html` | 사이드바에 전체 글 수 표시 |
| `_includes/head.html` | fediverse `meta` 태그 |
| `_includes/metadata-hook.html` | Pretendard 폰트 로드 (head 주입 지점) |
| `_data/category_labels.yml` | 홈 목록의 카테고리 배지 축약 라벨. 없는 카테고리는 이름 그대로 표시 |
| `_data/contact.yml` | 사이드바 연락처 아이콘. twitter는 계정 미확인으로 주석 처리 |
| `assets/css/jekyll-theme-chirpy.scss` | 커스텀 스타일 전부. `post-neighbors`(관련 글) / `home-*`(홈) / 디자인 레이어 3개 블록 |

### 이미 있으니 다시 만들지 말 것

- **카테고리별 글 탐색** → 카테고리 탭 (`_tabs/categories.md`) + `/categories/<slug>/`
- **태그별 탐색** → 태그 탭
- **전체 글 연대순** → 아카이브 탭
- **같은 카테고리 글 목록** → `_includes/related-posts.html`
- **검색** → Chirpy 기본 검색
- **댓글** → utterances (`_config.yml`). 라이트/다크 전환은 테마가 알아서 한다
- **이미지 라이트박스, 코드 복사 버튼, TOC, PWA** → 전부 Chirpy 기본

홈의 선반은 **쇼케이스이지 네비게이션이 아니다.** 탐색 기능을 홈에 복제하지 않는다.

---

## 3. 디자인 규약

- **액센트 색은 하나다.** electric blue: 라이트 `#0b5ed7`, 다크 `#6ea8fe`.
  섹션마다 다른 색을 쓰지 않는다. 새 색을 추가하려면 승인받는다.
- **라이트/다크 모두 정의한다.** 한쪽만 지원하는 스타일은 넣지 않는다.
  Chirpy는 `html[data-mode]` + `prefers-color-scheme` 4분기 구조를 쓴다.
  `assets/css/jekyll-theme-chirpy.scss`의 `accent-light` / `accent-dark` 믹스인을 따른다.
- **대비는 WCAG AA 이상.** 본문 4.5:1, 큰 글씨 3:1.
- **본문 폰트는 Pretendard.** 테마 기본 스택에는 한글 폰트가 없어 중국어 폰트로
  폴백된다. `_includes/metadata-hook.html`에서 로드한다.
- **한글 줄바꿈은 `word-break: keep-all`.** 없으면 단어 중간에서 잘린다.
- **모서리 반경은 한 체계로.** 현재 0.25~0.5rem 계열.
- **모션은 hover 전환 정도까지.** 스크롤 애니메이션, 무한 루프는 쓰지 않는다.
  모든 전환은 `prefers-reduced-motion: reduce`에서 꺼져야 한다.
- 새 CSS는 기존 클래스를 먼저 찾아보고, 없을 때만 추가한다. 이모지 대신
  Font Awesome 아이콘을 쓴다 (테마에 이미 포함).

---

## 4. 글 작성 규약

### 4.1 Front matter

```yaml
---
title: "[시리즈명] NN. 제목"
date: YYYY-MM-DD HH:MM:SS +0900
categories: [카테고리]
tags: [태그, 태그]
comments: true
math: true
toc: true
image:
  path: /assets/images/<시리즈>/<챕터>/<썸네일>.png
---
```

- 제목의 `[시리즈명]` 접두어는 홈 목록에서 자동으로 떨어진다 (배지와 중복이라).
- 이미지 경로는 `assets/images/<시리즈>/<챕터>/` 규칙을 지킨다.
- `pin: true`는 한 번에 한 글에만. 현재는 `OSSRL-01`.

### 4.2 본문

- **여백용 `<br>`을 쓰지 않는다.** 간격은 CSS가 만든다. 문장 중간의 진짜
  줄바꿈에만 쓴다.
- 이미지는 `<img>`만 쓰면 된다. 테마가 라이트박스로 감싸고, CSS가 블록 배치와
  상하 여백, 테두리를 준다.
- `##`(h2)로 섹션을 나눈다. h2에는 액센트 바가 붙어 스캔 기준선이 된다.
- 한 단락은 한 가지만 말한다. 길어지면 나눈다.
- 강조는 `<mark>`(액센트 배경) 또는 굵게. 색을 직접 지정한 `<span style="color:...">`는
  다크 모드에서 깨지므로 새로 쓰지 않는다.
- 수식은 `math: true` + `$...$` / `$$...$$`.
- 출처는 글 하단에 명시한다.

---

## 5. 카테고리 / 태그 규약

- **카테고리는 Title Case.** 현재: `Algorithm`, `GitHub Pages`,
  `Reinforcement Learning`, `Setup`.
- **카테고리를 함부로 늘리지 않는다.** 글 1편짜리 카테고리는 만들지 않는다.
  시리즈가 될 게 확실할 때만 새로 판다.
- **카테고리 이름을 바꾸면 URL이 바뀔 수 있다.** `slugify` 결과가 같은지
  확인하고, 달라지면 승인받는다.
- 카테고리를 추가/변경하면 `_data/category_labels.yml`의 배지 라벨도 갱신한다.
- **태그는 대소문자와 표기를 통일한다.** slug가 겹치면 (`github` vs `Github`)
  빌드가 한쪽을 덮어써서 글이 목록에서 사라진다.
- 같은 개념을 여러 태그로 나누지 않는다 (`sort` / `sorting` 금지).

---

## 6. 작업 절차

```bash
bundle exec jekyll build                        # 검증용 빌드
bundle exec jekyll serve --livereload --port 4000   # 로컬 미리보기
```

작업을 끝냈다고 보고하기 전에:

1. **빌드가 경고 없이 끝나는가.** `Conflict:` 경고는 실패로 간주한다.
2. **내부 링크가 살아 있는가.** 특히 카테고리/태그 URL을 건드렸을 때.
3. **라이트/다크 양쪽에서 확인했는가.**
4. **모바일 폭(< 576px)에서 깨지지 않는가.**
5. **죽은 코드를 남기지 않았는가.** 없앤 마크업의 CSS도 같이 지운다.

검증하지 않은 것을 "됐다"고 보고하지 않는다. 못 한 검증은 못 했다고 말한다.

---

## 7. 하지 말 것

- 승인 없는 `git commit` / `git push`. 커밋 메시지는 제안만 한다.
- gem 안의 테마 원본 파일 수정. 필요하면 해당 파일을 저장소로 복사한 뒤 수정하고,
  2절 인벤토리에 추가한다.
- 새 의존성(gem, npm, CDN) 추가. 몇 줄로 되는 일에는 더더욱.
- 작성자가 쓴 글의 내용을 임의로 수정. 오탈자도 보고 후 승인받는다.
- 확인 안 된 정보를 글이나 About 페이지에 쓰기. 모르면 빈칸으로 두고 묻는다.
- URL 구조, 글 slug, 카테고리 이름을 조용히 바꾸기.
